import { Routes } from '@angular/router';
import { ConfirmationDialogComponent } from 'app/modules/ui/confirmation-dialog/confirmation-dialog.component';

export default [
    {
        path     : '',
        component: ConfirmationDialogComponent,
    },
] as Routes;
